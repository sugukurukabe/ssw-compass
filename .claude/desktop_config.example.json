{
  "_comment": "Sample config for Claude Desktop. Copy this file to ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) — or merge its mcpServers entry if you already have a config there — and replace YOUR_TUNNEL_URL with your actual Cloudflare tunnel URL (e.g. https://abc-def-123.trycloudflare.com). Restart Claude Desktop after editing.",
  "mcpServers": {
    "ssw-local": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "YOUR_TUNNEL_URL/mcp"
      ]
    }
  }
}
